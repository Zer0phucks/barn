# Vercel web UI dependencies only.
# For full scanner setup (Playwright, AI libs, etc.) see requirements-scan.txt
# and run ./install.sh on the local VM.
flask>=2.0.0
# supabase<2.23 avoids storage3>=2.23 which introduced a pyiceberg dependency
# that requires gcc to compile and is unavailable in the Vercel build environment.
supabase>=2.0.0,<2.23
python-dotenv>=1.0.0
requests>=2.0.0
